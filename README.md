# Origins Ascendants
An origins plugin for the Origins Ascendants Minecraft server

The GUI isn't developed yet, so use the `/setorigin` command 

`/setorigin player ORIGIN` where the origin is in all caps, such as ELYTRIAN.
